**1.1 Install Missing DHCP Client**
```bash
# In VM console:
apk add --no-cache udhcpc
rc-update add udhcpc default
```

**1.2 Fix Network Configuration**
```bash
# Replace /etc/network/interfaces:
cat > /etc/network/interfaces << 'EOF'
auto lo
iface lo inet loopback

auto eth0  
iface eth0 inet dhcp
    udhcpc_opts -h alpine-ssh
EOF

# Restart networking:
rc-service networking restart
```

**1.3 Verify Virtio Modules**
```bash
# Check if virtio modules are loaded:
lsmod | grep virtio
modprobe virtio_net virtio_pci  # Load if missing
```

### Phase 2: Service Dependencies

**2.1 Fix Service Start Order**
```bash
# Ensure proper boot sequence:
rc-update add localmount boot
rc-update add networking boot  
rc-update add hostname boot
rc-update add sshd default
rc-update add qemu-guest-agent default

# Restart services in correct order:
rc-service networking restart
rc-service hostname restart  
rc-service sshd restart
rc-service qemu-guest-agent restart
```

**2.2 SSH Configuration Verification**
```bash
# Test SSH config syntax:
sshd -t
# Fix permissions if needed:
chmod 600 /etc/ssh/ssh_host_*_key
chmod 644 /etc/ssh/ssh_host_*_key.pub
```

### Phase 3: Build Script Corrections

Create improved build script (`build-alpine-image-final.sh`):

**Key Changes Needed in setup-alpine-ssh.sh section:**
```bash
# Add DHCP client installation:
apk add --no-cache \
    openssh openssh-server openssh-client \
    sudo qemu-guest-agent curl wget bash openrc \
    udhcpc  # <- ADD THIS

# Fix network config:
cat > /etc/network/interfaces << 'NETCFG'
auto lo
iface lo inet loopback

auto eth0
iface eth0 inet dhcp
    udhcpc_opts -h alpine-ssh
NETCFG

# Ensure proper service dependencies:
rc-update add localmount boot
rc-update add networking boot
rc-update add hostname boot  
rc-update add udhcpc boot    # <- ADD THIS
rc-update add sshd default
rc-update add qemu-guest-agent default
```

### Phase 4: Ansible Role Fixes

**4.1 Fix Service Module Usage**
Replace all `service:` tasks with Alpine-compatible versions:

```yaml
# OLD (systemd-based):
- name: Enable and start Docker
  service:
    name: docker
    state: started  
    enabled: yes

# NEW (OpenRC-based):
- name: Enable and start Docker
  shell: |
    rc-update add docker default
    rc-service docker start
  args:
    creates: /run/openrc/started/docker
```

**4.2 Package Manager Corrections**
The role correctly uses `apk:` module, but ensure Alpine-specific package names:
- `docker` → `docker docker-compose` 
- `ufw` → `iptables iptables-legacy` (Alpine doesn't have UFW)

### Phase 5: Validation Workflow

**5.1 Network Connectivity Test**
```bash
# Create test script:
cat > /tmp/test-alpine-connectivity.sh << 'EOF'
#!/bin/bash
ALPINE_IPS="192.168.0.163 192.168.0.236"  # Known edge VM IPs

for ip in $ALPINE_IPS; do
    echo "Testing $ip..."
    timeout 5 ssh -i ~/.ssh/id_ed25519_clarity \
        -o ConnectTimeout=3 -o StrictHostKeyChecking=no \
        VespianRex@$ip 'hostname && echo "SUCCESS: Alpine reachable at $ip"'
done
EOF

chmod +x /tmp/test-alpine-connectivity.sh
/tmp/test-alpine-connectivity.sh
```

**5.2 Ansible Connectivity Verification**  
```bash
# Test Ansible can reach Alpine hosts:
ansible -i inventory edge -m ping -u VespianRex \
    --private-key ~/.ssh/id_ed25519_clarity
```

### Phase 6: Proxmox Settings Verification

**6.1 VM Configuration Check**
```bash
# On Proxmox host:
qm config 9008
# Verify:
# - net0: virtio,bridge=vmbr0 (correct)
# - agent: 1 (enabled) 
# - boot: order=scsi0 (correct)
```

**6.2 Network Bridge Status**
```bash  
# Check bridge connectivity:
ip addr show vmbr0
brctl show vmbr0
```

## Execution Priority

1. **IMMEDIATE**: Console access and network fixes (Phase 1-2)
2. **SHORT-TERM**: Build script improvements (Phase 3) 
3. **MEDIUM-TERM**: Ansible role corrections (Phase 4)
4. **VALIDATION**: Connectivity testing (Phase 5-6)

## Fallback Strategy

If Alpine issues persist after these fixes:
1. **Hybrid approach**: Use working Debian VMs for critical edge services
2. **Selective Alpine**: Reserve Alpine only for specific lightweight containers
3. **Template refinement**: Consider Alpine 3.19 LTS vs 3.20 for stability

This structured approach addresses the root causes while maintaining Debian as a reliable fallback option.
